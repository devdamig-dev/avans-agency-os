import { Badge,Card } from "./ui"; import { MoreHorizontal } from "lucide-react";
export function DataTable({columns,rows}:{columns:string[];rows:(string|React.ReactNode)[][]}){return <Card className="overflow-hidden p-0"><div className="overflow-x-auto"><table className="w-full text-left"><thead><tr className="border-b border-avans-border bg-avans-bg/60">{columns.map(c=><th key={c} className="whitespace-nowrap px-5 py-3 text-[11px] font-bold uppercase tracking-wider text-avans-muted">{c}</th>)}<th/></tr></thead><tbody>{rows.map((r,i)=><tr key={i} className="border-b border-avans-border last:border-0 hover:bg-avans-bg/40">{r.map((v,j)=><td key={j} className="whitespace-nowrap px-5 py-4 text-sm">{j===r.length-1?<Badge tone={String(v).match(/Activo|Nuevo|progreso|Alta/) ? "success":"neutral"}>{v}</Badge>:v}</td>)}<td className="px-5"><MoreHorizontal size={18} className="text-avans-muted"/></td></tr>)}</tbody></table></div></Card>}
