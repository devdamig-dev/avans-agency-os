import { Button } from "./ui"; import { Plus } from "lucide-react";
export function PageHeading({eyebrow,title,description,action}:{eyebrow:string;title:string;description:string;action?:string}){return <div className="mb-7 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-avans-purple">{eyebrow}</p><h1 className="mt-2 font-display text-3xl font-bold md:text-4xl">{title}</h1><p className="mt-2 max-w-2xl text-sm text-avans-muted">{description}</p></div>{action&&<Button><Plus size={16} className="mr-2"/>{action}</Button>}</div>}
