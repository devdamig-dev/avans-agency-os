import {PageHeading} from "./page-heading";import {Badge,Card} from "./ui";import {ArrowRight,Sparkles} from "lucide-react";
export function ModulePlaceholder({eyebrow,title,description,items}:{eyebrow:string;title:string;description:string;items:string[]}){return <><PageHeading eyebrow={eyebrow} title={title} description={description} action={`Nuevo ${title.toLowerCase()}`}/><div className="grid gap-4 md:grid-cols-3">{items.map((x,i)=><Card key={x}><div className="flex justify-between"><Badge tone={i===0?"warning":"purple"}>{i===0?"Pendiente":"Borrador IA"}</Badge><Sparkles size={17} className="text-avans-magenta"/></div><h3 className="mt-5 font-semibold">{x}</h3><p className="mt-2 text-sm text-avans-muted">Información preparada para que el equipo revise y defina el próximo paso.</p><button className="mt-5 flex items-center gap-2 text-xs font-bold text-avans-purple">Abrir detalle <ArrowRight size={14}/></button></Card>)}</div></>}
