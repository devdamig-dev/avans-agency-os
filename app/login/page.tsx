import { Logo } from "@/components/logo"; import { Button,Input } from "@/components/ui"; import { login } from "./actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="grid min-h-screen lg:grid-cols-[1.1fr_.9fr]">
 <section className="gradient-dark relative hidden overflow-hidden p-14 text-white lg:flex lg:flex-col lg:justify-between"><Logo dark/><div className="relative z-10 max-w-xl"><p className="mb-5 text-sm font-bold uppercase tracking-[.25em] text-orange-300">Marketing que funciona</p><h1 className="font-display text-6xl font-bold leading-[1.03]">La operación detrás de las <span className="gradient-text">grandes ideas.</span></h1><p className="mt-6 max-w-md text-lg leading-8 text-white/65">Automatización interna para una agencia que quiere operar mejor, sin perder el control humano.</p></div><p className="text-sm text-white/40">La IA prepara. El equipo aprueba.</p></section>
 <section className="flex items-center justify-center p-7"><div className="w-full max-w-md"><div className="mb-10 lg:hidden"><Logo/></div><p className="text-sm font-semibold text-avans-purple">Bienvenido al equipo</p><h2 className="mt-2 font-display text-4xl font-bold">Entrá a tu espacio</h2><p className="mt-3 text-avans-muted">Todo lo importante, visible y bajo control.</p><form action={login} className="mt-9 space-y-5"><label className="block text-sm font-semibold">Email<Input name="email" type="email" required placeholder="vos@avans.agency" className="mt-2"/></label><label className="block text-sm font-semibold">Contraseña<Input name="password" type="password" required placeholder="••••••••" className="mt-2"/></label>{error&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<Button className="w-full">Ingresar a Agency OS</Button></form><p className="mt-8 text-center text-xs text-avans-muted">Acceso exclusivo para el equipo de Avans Agency.</p></div></section></main>}
