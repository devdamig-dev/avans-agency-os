import { PageHeading } from "@/components/page-heading";import {Badge,Card} from "@/components/ui";import {ArrowUpRight,Bot,CheckSquare,ClipboardCheck,Contact,FileText,Users} from "lucide-react";
const metrics=[["Leads nuevos","12","+18%",Contact],["Clientes activos","24","3 en onboarding",Users],["En aprobación","8","Requieren acción",ClipboardCheck],["Tareas vencidas","3","Resolver hoy",CheckSquare]];
export default function Dashboard(){return <><PageHeading eyebrow="Centro de operaciones" title="Buen día, equipo." description="Una vista clara para decidir qué necesita atención y qué puede seguir avanzando."/><Card className="relative mb-6 overflow-hidden border-0 bg-avans-dark p-7 text-white"><div className="absolute inset-0 bg-gradient-to-r from-orange-500/20 via-fuchsia-500/20 to-purple-600/30"/><div className="relative flex flex-col justify-between gap-6 md:flex-row md:items-center"><div><Badge tone="warning">Marketing que funciona</Badge><h2 className="mt-4 font-display text-3xl font-bold">Nada queda perdido.</h2><p className="mt-2 max-w-xl text-sm text-white/65">La IA prepara borradores y próximos pasos. Tu equipo mantiene siempre la última palabra.</p></div><div className="rounded-2xl border border-white/10 bg-white/10 px-6 py-4 backdrop-blur"><p className="text-xs text-white/50">Agentes activos</p><p className="mt-1 text-3xl font-bold">8 <span className="text-sm font-normal text-emerald-300">operando</span></p></div></div></Card><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(([label,value,note,Icon])=><Card key={label as string}><div className="flex items-start justify-between"><div className="rounded-xl bg-gradient-to-br from-orange-50 to-purple-50 p-2.5"><Icon size={20} className="text-avans-purple"/></div><ArrowUpRight size={16} className="text-avans-muted"/></div><p className="mt-5 text-sm text-avans-muted">{label as string}</p><p className="mt-1 font-display text-3xl font-bold">{value as string}</p><p className="mt-2 text-xs text-avans-muted">{note as string}</p></Card>)}</div><div className="mt-6 grid gap-6 xl:grid-cols-[1.4fr_.6fr]"><Card><div className="flex items-center justify-between"><h3 className="font-display text-lg font-bold">Actividad reciente</h3><button className="text-xs font-semibold text-avans-purple">Ver todo</button></div><div className="mt-5 space-y-5">{[["Propuesta lista para revisión","Fundación Horizonte · hace 12 min",FileText],["Lead analizado por IA","Nativa Wellness · hace 34 min",Bot],["Tarea completada","Calendario Marea Café · hace 1 h",CheckSquare]].map(([t,s,Icon])=><div className="flex gap-3" key={t as string}><div className="rounded-xl bg-avans-bg p-2"><Icon size={17}/></div><div><p className="text-sm font-semibold">{t as string}</p><p className="text-xs text-avans-muted">{s as string}</p></div></div>)}</div></Card><Card><h3 className="font-display text-lg font-bold">Próximo foco</h3><div className="mt-5 rounded-xl bg-orange-50 p-4"><p className="text-sm font-semibold">3 tareas vencidas</p><p className="mt-1 text-xs leading-5 text-avans-muted">Destrabá pendientes antes de iniciar trabajo nuevo.</p></div><div className="mt-3 rounded-xl bg-purple-50 p-4"><p className="text-sm font-semibold">8 borradores esperan revisión</p><p className="mt-1 text-xs leading-5 text-avans-muted">Nada se envía sin tu aprobación.</p></div></Card></div></>}
